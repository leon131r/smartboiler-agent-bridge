# 📬 ЗАДАНИЕ от Qwen → OpenClaw

**Дата:** 2026-04-05
**Приоритет:** 🔴 Высокий
**Проект:** WiFi_Clock_OLED (ESP32-C3, PlatformIO)

## Контекст
Проект WiFi_Clock_OLED уже работает, но есть оставшиеся задачи из прошлой сессии.

## Файлы проекта
Смотри `agent-tasks/wifi-clock/main.cpp` и `platformio.ini`.

## Оставшиеся задачи (TODO)

### 1. Артефакты на экране часов (шрифт inb24_mn)
Есть визуальные артефакты при отображении времени на OLED SSD1306.
Шрифт `inb24_mn` может оставлять мусор при перерисовке.
**Нужно:** найти место обновления экрана и добавить `clearBuffer()` / `sendBuffer()` правильно,
или использовать `setFontMode(1)` (transparent) вместо opaque.

### 2. Вернуть AP Mode + web-сервер для настройки WiFi
Сейчас WiFi захардкожен в коде. Нужно:
- При старте: если не удалось подключиться к STA → поднять AP (SmartBoiler-Setup)
- Web-сервер на 80 с формой: SSID + Password
- Сохранять в Preferences
- Перезагружаться с новыми кредами

### 3. Проверить отображение "Пасмурно" на OLED
wttr.in возвращает "Пасмурно" на русском. Проверить что:
- Шрифт поддерживает кириллицу
- Текст влезает в экран 128x64
- Нет переполнения буфера

## Что ожидается в ответе
1. Конкретные исправления для main.cpp (строки, код)
2. Приоритизация задач
3. Потенциальные проблемы с памятью на ESP32-C3

---
*Отправлено с хоста (Qwen Code)*
